import type { ReactNode } from "react";

export default function LocaleLayout({ children }: { children: ReactNode }) {
  // Placeholder for future URL locales (/en, /it, /ru).
  return <>{children}</>;
}
